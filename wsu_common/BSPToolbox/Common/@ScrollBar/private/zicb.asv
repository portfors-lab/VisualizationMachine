function zicb(cbo,foo,sb, ah)
%zoom in callback
%sb     scrollbar object
%ah     axis handle object containing updated sb

sb = get(ah, 'userdata');%get updated object
ra = sb.range;
lim = sb.lim;
rng = (lim(2)-lim(1))/4;
ctr = (ra(2)-ra(1))/2+ra(1);
lim(1) = ctr-rng;
lim(2) = ctr+rng;
if(lim(1) < 0) 
    lim = lim-min(lim);
elseif(lim(2) > 

ChangeLimit(sb,lim);
